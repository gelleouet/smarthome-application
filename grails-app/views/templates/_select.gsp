<g:select name="${ params.name }" from="${params.values}"  optionKey="${ params.optionKey }" 
	optionValue="${ params.optionValue }"
	value="${params.selectValue}" class="select"
	size="${ params.size }" style="${ params.style }" multiple="${ params.multiple }"
	required="${ params.required }"
	noSelection="${ params.noSelection }"
	/>
